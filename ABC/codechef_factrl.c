/*

        scanf("%hi", &n);
        double factorial = 1;
        while (n != 0)
        {
            factorial = (double)factorial * (n);
            n--;
        }

        printf("%.0lf\n", factorial);
    }

    return 0;
}*/
#include<stdio.h>

int main(){
// float f =3.2;
// if(f == 3.2)
// printf("c is simple");

// else
// printf("c is complicated");

int f = 'a';
printf(" %d", f);
}